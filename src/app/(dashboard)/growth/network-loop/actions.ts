"use server";import {revalidatePath} from "next/cache";import {requireUser} from "@/lib/rbac/guard";import {promoteFriction} from "@/lib/growth/nervous-system";export async function promoteFrictionAction(f:FormData){const u=await requireUser();const id=f.get("frictionId");if(typeof id!=="string"||!id)throw new Error("Friction evidence is required");await promoteFriction({actorUserId:u.id,frictionId:id});revalidatePath("/growth/network-loop");revalidatePath("/growth");revalidatePath("/today");}
