"use client";export default function DashboardError({reset}:{error:Error&{digest?:string};reset:()=>void}){return <section role="alert" className="mx-auto max-w-2xl rounded-[30px] border border-accent/25 bg-surface-raised p-8"><p className="text-[10px] font-semibold uppercase tracking-[.2em] text-accent">Workspace unavailable</p><h1 className="mt-3 font-display text-3xl text-ink">This room could not load safely.</h1><p className="mt-3 text-sm leading-6 text-ink-muted">No action was assumed. Check your connection and try again; if the problem remains, open Operations so the failure can be handled explicitly.</p><div className="mt-5 flex gap-2"><button onClick={reset} className="rounded-xl bg-brand px-4 py-2 text-sm font-semibold text-white">Try again</button><a href="/operations" className="rounded-xl border border-surface-border px-4 py-2 text-sm font-semibold text-ink">Open Operations</a></div></section>}
