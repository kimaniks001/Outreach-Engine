"use client";import {useEffect,useState} from "react";export function ResilienceNotice(){const [offline,setOffline]=useState(false);useEffect(()=>{const sync=()=>setOffline(!navigator.onLine);sync();window.addEventListener("online",sync);window.addEventListener("offline",sync);return()=>{window.removeEventListener("online",sync);window.removeEventListener("offline",sync)}},[]);if(!offline)return null;return <div role="status" className="mb-4 rounded-2xl border border-accent/30 bg-accent-soft/30 px-4 py-3 text-sm text-ink">You are offline. Existing content may remain visible, but changes and fresh SecurePay authority checks will wait for connection.</div>}
