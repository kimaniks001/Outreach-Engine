import {existsSync,readFileSync,statSync} from "node:fs";import {resolve} from "node:path";
const root=process.cwd();const required=["src/app/(dashboard)/today/page.tsx","src/app/(dashboard)/conversations/page.tsx","src/app/(dashboard)/work/page.tsx","src/app/(dashboard)/traders/page.tsx","src/app/(dashboard)/growth/page.tsx","src/app/(dashboard)/operations/page.tsx","src/app/(dashboard)/people/page.tsx","src/app/(dashboard)/copilot/page.tsx","src/app/(dashboard)/loading.tsx","src/app/(dashboard)/error.tsx","docs/INITIAL_LAUNCH_RUNBOOK.md","docs/TRUE_NORTH_PHASE_10_PRODUCTION_CLOSURE.md"];
const missing=required.filter(p=>!existsSync(resolve(root,p)));if(missing.length)throw new Error(`Production closure missing: ${missing.join(", ")}`);
const manifestPath=resolve(root,".next/build-manifest.json");if(!existsSync(manifestPath))throw new Error("Run production build before closure verification");const manifest=JSON.parse(readFileSync(manifestPath,"utf8")) as {pages?:Record<string,string[]>};const chunks=new Set(Object.values(manifest.pages??{}).flat().filter(x=>x.endsWith(".js")));let bytes=0;for(const chunk of chunks){const path=resolve(root,".next",chunk);if(existsSync(path))bytes+=statSync(path).size;}const budget=3*1024*1024;if(bytes>budget)throw new Error(`Shared client JavaScript ${bytes} bytes exceeds ${budget} byte launch budget`);console.log(JSON.stringify({status:"PASS",requiredJourneys:required.length,sharedClientJavaScriptBytes:bytes,budgetBytes:budget}));
